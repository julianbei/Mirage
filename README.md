# Loreworks Browser Game Framework

Client-side WASM simulation + Babylon.js renderer. Optional P2P multiplayer. Lockstep gameplay with soft-host deltas for late join/recovery.

- Dev: `pnpm i && pnpm dev`
- Build: `pnpm build`

WASM: Rust via `wasm-bindgen`. See `packages/wasm-sim`.
